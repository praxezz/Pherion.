# ---- Required for capture ----
scapy>=2.5.0           # packet capture/parsing (sniffing, protocol dissection)

# ---- Required for ML engine ----
numpy>=1.24.0          # per-protocol baseline math, feature vectors
pandas>=2.0.0           # CSV dataset loading for ML training (CIC-IDS/NSL-KDD/UNSW-NB15)
scikit-learn>=1.3.0     # RandomForestClassifier, scaling, train/test split, metrics
joblib>=1.3.0           # model serialization (save/load trained baselines)

# ---- Optional ----
psutil>=5.9.0           # system health watchdog (CPU/memory/thread monitoring)
imbalanced-learn>=0.11.0  # SMOTE oversampling for imbalanced ML training sets
geoip2>=4.7.0           # GeoIP enrichment of alert source IPs (needs GeoLite2-City.mmdb)

# ---- GUI ----
# tkinter ships with most Python installs; if missing on Linux:
#   Debian/Ubuntu: sudo apt install python3-tk
#   Fedora:        sudo dnf install python3-tkinter

# ---- Platform notes ----
# Windows: packet capture requires Npcap (https://npcap.com) installed alongside scapy
# Linux/macOS: requires libpcap (usually preinstalled) and root/sudo for raw sockets
